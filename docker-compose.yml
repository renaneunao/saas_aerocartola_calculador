services:
  calculador:
    image: renaneunao/saas-cartola-calculador:latest
    container_name: cartola-aero-calculador-container
    restart: unless-stopped
    env_file:
      - .env
    environment:
      POSTGRES_HOST: ${POSTGRES_HOST}
      POSTGRES_PORT: ${POSTGRES_PORT:-5432}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
      NORMAL_CALCULATION_INTERVAL_MINUTES: ${NORMAL_CALCULATION_INTERVAL_MINUTES:-30}
      CLOSING_DAY_CALCULATION_INTERVAL_MINUTES: ${CLOSING_DAY_CALCULATION_INTERVAL_MINUTES:-5}
    volumes:
      - ./logs:/app/logs
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    networks:
      - infra_network

networks:
  infra_network:
    external: true

